[package]
name = "kalc"
version = "0.5.1"
edition = "2021"
[dependencies]
console      = { version = "*", default-features = false }
gnuplot      = { version = "*", default-features = false }
[target.'cfg(target_os = "linux")'.dependencies]
libc         = { version = "*", default-features = false }